'use strict';

const translations = window.translations;

const arrayPhrases = Object.entries(translations);
const lenArrayPhrases = arrayPhrases.length;
console.log(lenArrayPhrases);

// console.log(translations);
